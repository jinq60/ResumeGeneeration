# 根配置：停止向上层目录冒泡查找 lombok.config
config.stopBubbling = true

# 让 @Lazy 等注解在 Lombok 生成的构造器参数上被复制，
# 修复 @RequiredArgsConstructor / @AllArgsConstructor 场景下
# 字段级 @Lazy 不生效（导致循环依赖无法被代理延迟打破）的问题
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
